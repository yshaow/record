/**
 * 为了方便，自定义js模块，用于获取NativeModules中的模块
 */
import {NativeModules} from 'react-native'

//ToastExample 是getName中的返回值
// export default NativeModules.ToastExample;
export default NativeModules.ImagePickerModule;
